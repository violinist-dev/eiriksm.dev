language: php
php:
  - "7.2"

sudo: false

addons:
  chrome: stable

matrix:
  fast_finish: true

before_script:
  - google-chrome --version
  - /bin/bash scripts/travis.sh

script:
  - composer test
